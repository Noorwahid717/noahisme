---
import Base from "~/layouts/Base.astro";
import { SITE } from "~/lib/seo";

const structuredData = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Intro Call (20m)",
  description:
    "Sesi perkenalan, bahas kebutuhan awal, budget range, dan rekomendasi solusi teknis yang tepat.",
  provider: {
    "@type": "Person",
    name: "Noah Wahid",
  },
  url: `${SITE.url}/book/intro`,
};

const whatsappIntroMessage = encodeURIComponent(`Hi, I'd like to book an Intro Call (20m).
Name: 
Project/Need: 
Preferred time: `);

const emailSubject = encodeURIComponent("Intro Call Request");
const emailBody = encodeURIComponent(`Name: 
Project: 
Preferred time: `);
---

<Base
  title="Intro Call (20m)"
  description="Sesi perkenalan untuk validasi kebutuhan dan cek kecocokan timeline proyek."
  structuredData={structuredData}
  breadcrumbs={[
    { name: "Book", url: "/book/intro" },
    { name: "Intro Call", url: "/book/intro" },
  ]}
>
  <section class="mx-auto grid max-w-4xl gap-10 px-6 pb-24 pt-28">
    <header class="space-y-4">
      <p class="text-xs uppercase tracking-[0.3em] text-accent">Intro Call</p>
      <h1 class="font-heading text-4xl text-text">Intro Call (20m)</h1>
      <p class="max-w-2xl text-base text-text2">
        Sesi perkenalan, bahas kebutuhan awal, budget range, dan rekomendasi solusi teknis yang
        tepat. Cocok jika kamu masih eksplor atau belum punya scope jelas.
      </p>
    </header>

    <div class="grid gap-8 rounded-3xl border border-divider/40 bg-surface/80 p-8 shadow-soft">
      <div class="space-y-4">
        <h2 class="text-xl font-semibold text-text">Apa yang akan kita bahas?</h2>
        <ul class="grid gap-3 text-sm text-text2">
          <li class="flex items-start gap-3">
            <svg
              class="mt-0.5 h-5 w-5 flex-shrink-0 text-accent"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 13l4 4L19 7"></path>
            </svg>
            <span>Saling mengenal dan validasi kebutuhan proyek</span>
          </li>
          <li class="flex items-start gap-3">
            <svg
              class="mt-0.5 h-5 w-5 flex-shrink-0 text-accent"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 13l4 4L19 7"></path>
            </svg>
            <span>Diskusi budget range dan timeline yang realistis</span>
          </li>
          <li class="flex items-start gap-3">
            <svg
              class="mt-0.5 h-5 w-5 flex-shrink-0 text-accent"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 13l4 4L19 7"></path>
            </svg>
            <span>Rekomendasi pendekatan terbaik (Fixed Project, Sprint, atau Hourly)</span>
          </li>
          <li class="flex items-start gap-3">
            <svg
              class="mt-0.5 h-5 w-5 flex-shrink-0 text-accent"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 13l4 4L19 7"></path>
            </svg>
            <span>Next steps jika kita cocok untuk bekerja sama</span>
          </li>
        </ul>
      </div>

      <div id="calendar" class="space-y-4">
        <h2 class="text-xl font-semibold text-text">Pilih Jadwal</h2>
        <div class="aspect-[16/9] w-full overflow-hidden rounded-xl border border-divider/40">
          <iframe
            src="https://cal.com/noahisme/intro"
            class="h-full w-full"
            frameborder="0"
            title="Book Intro Call"></iframe>
        </div>
      </div>

      <div class="space-y-4 border-t border-divider/40 pt-8">
        <h3 class="text-lg font-semibold text-text">Tidak menemukan jadwal yang cocok?</h3>
        <p class="text-sm text-text2">Hubungi langsung melalui:</p>
        <div class="flex flex-wrap gap-3">
          <a
            href={`https://wa.me/6281335843507?text=${whatsappIntroMessage}`}
            target="_blank"
            rel="noreferrer"
            class="focus-outline inline-flex items-center gap-2 rounded-lg border border-divider/60 bg-bg/80 px-4 py-2.5 text-sm text-text transition-colors hover:border-accent hover:text-accent"
          >
            <svg
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
            WhatsApp
          </a>
          <a
            href={`mailto:noorwahid717@gmail.com?subject=${emailSubject}&body=${emailBody}`}
            class="focus-outline inline-flex items-center gap-2 rounded-lg border border-divider/60 bg-bg/80 px-4 py-2.5 text-sm text-text transition-colors hover:border-accent hover:text-accent"
          >
            <svg
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <rect x="2" y="4" width="20" height="16" rx="2"></rect>
              <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"></path>
            </svg>
            Email
          </a>
        </div>
      </div>
    </div>
  </section>
</Base>
